{
  "name": "paltalabs",
  "version": "1.0.0",
  "description": "Paltalabs - Comprehensive DeFi platform on Stellar blockchain",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:contracts && npm run build:backend && npm run build:frontend",
    "build:contracts": "cd contracts && cargo build --target wasm32-unknown-unknown --release",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "deploy": "npm run deploy:contracts && npm run deploy:backend && npm run deploy:frontend",
    "test": "npm run test:contracts && npm run test:backend && npm run test:frontend",
    "lint": "eslint . --ext .js,.ts,.tsx",
    "format": "prettier --write ."
  },
  "keywords": [
    "defi",
    "stellar",
    "blockchain",
    "yield",
    "staking",
    "liquidity"
  ],
  "author": "Paltalabs Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "typescript": "^5.3.3"
  },
  "workspaces": [
    "contracts",
    "backend",
    "frontend",
    "mobile"
  ]
}
